import 'package:flutter/material.dart';

class IconConstants {
  static const Icon emailIcon = Icon(Icons.email_outlined, size: 24);
  static const Icon passwordIcon = Icon(Icons.password, size: 24);
  static const Icon passwordVisible = Icon(Icons.visibility_outlined, size: 24);
  static const Icon passwordVisibleOff = Icon(
    Icons.visibility_off_outlined,
    size: 24,
  );
  static const Icon logoutIcon = Icon(Icons.logout, size: 24);
}
